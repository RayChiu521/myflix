%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @follows.each do |follow|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(follow.user.email.downcase)}?s=40")
            = link_to follow.user.full_name, follow.user
          %td.extra-padding= follow.user.queue_items.count
          %td.extra-padding= follow.user.followers.count
          %td.extra-padding
            = link_to follow, method: :delete do
              %i.glyphicon.glyphicon-remove
